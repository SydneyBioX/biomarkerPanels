% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objectives.R
\name{define_ruleout_objectives}
\alias{define_ruleout_objectives}
\title{Define Objectives with Neyman-Pearson Sensitivity Constraint}
\usage{
define_ruleout_objectives(
  losses = c("num_features", "specificity"),
  sens_constraint = 0.9,
  sens_cutoff = 0.5,
  params = list(),
  ...
)
}
\arguments{
\item{losses}{Character vector of loss names to optimize. Should typically
exclude \code{"sensitivity"} when using constraint mode; if included, it will
be removed with a warning.}

\item{sens_constraint}{Minimum sensitivity threshold. Solutions below this
value receive infinite objective penalties and are excluded. Set to \code{NULL}
to disable constraint mode.}

\item{sens_cutoff}{Classification probability cutoff for sensitivity

calculation (default 0.5).}

\item{params}{Named list of additional parameters passed to
\code{\link[=define_objectives]{define_objectives()}} (e.g., \code{list(pauc = list(sens_floor = 0.90))}).}

\item{...}{Additional arguments passed to \code{\link[=define_objectives]{define_objectives()}}.}
}
\value{
A list with two elements:
\describe{
\item{objectives}{Named list of objective descriptors for \code{\link[=optimize_panel]{optimize_panel()}}.}
\item{constraints}{List of constraint descriptors for \code{\link[=optimize_panel]{optimize_panel()}}.}
}
}
\description{
Convenience wrapper for "rule-out" diagnostic optimization. Instead of
treating sensitivity as an objective to be traded off, it becomes a hard
constraint: solutions with sensitivity below \code{sens_constraint} are excluded
from the Pareto front. This focuses optimization on clinically valid
rule-out solutions only.
}
\examples{
# Optimize panel size and pAUC, requiring sensitivity >= 0.95
config <- define_ruleout_objectives(
  losses = c("num_features", "pauc"),
  sens_constraint = 0.95,
  params = list(pauc = list(sens_floor = 0.90))
)

# Use with optimize_panel:
# panel <- optimize_panel(x, y,
#   objectives = config$objectives,
#   constraints = config$constraints
# )
}
