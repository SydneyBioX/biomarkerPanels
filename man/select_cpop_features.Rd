% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpop_select.R
\name{select_cpop_features}
\alias{select_cpop_features}
\title{Select Features via the CPOP Algorithm}
\usage{
select_cpop_features(
  x_list,
  y_list,
  n_features = 50L,
  n_iter = 20L,
  alpha = 1,
  lambda_choice = c("lambda.min", "lambda.1se"),
  penalty = NULL,
  sign_consistency_threshold = 1,
  prune_collinear = TRUE,
  family = "binomial",
  assay = NULL,
  cv_control = list()
)
}
\arguments{
\item{x_list}{A matrix-like object, \code{SummarizedExperiment}, or list of such
objects. Rows are samples, columns are features. Original CPOP is defined
for two cohorts; this implementation generalises by intersecting selections
across all cohorts at every CPOP step.}

\item{y_list}{A binary response factor aligned with \code{x_list} (levels
\code{c("No", "Yes")}). Provide a list when \code{x_list} is a list.}

\item{n_features}{Maximum number of pair features to retain (default \code{50}).}

\item{n_iter}{Maximum number of iterations for both steps (default \code{20}).}

\item{alpha}{Elastic-net mixing parameter for step 1. Default \code{1} (lasso).
A numeric vector triggers CPOP's alpha sweep: features carried over from
earlier alphas are exempted from the penalty in later passes.}

\item{lambda_choice}{Either \code{"lambda.min"} (default) or \code{"lambda.1se"}.}

\item{penalty}{Optional numeric vector of \code{glmnet} \code{penalty.factor} values
named by pair feature. When \code{NULL}, \code{\link[=colmeans_penalty]{colmeans_penalty()}} is used, matching
the CPOP default.}

\item{sign_consistency_threshold}{Minimum fraction of cohorts required to
agree on coefficient sign in step 2. Default \code{1.0} is the canonical CPOP
rule (unanimous agreement).}

\item{prune_collinear}{Logical; if \code{TRUE} (default), removes redundant
ratios by retaining a minimum spanning tree of the implied feature graph
(matching CPOP's \code{mst_lratio}).}

\item{family}{\code{glmnet} family. Default \code{"binomial"}.}

\item{assay}{For \code{SummarizedExperiment} inputs, the assay name or index.}

\item{cv_control}{Optional named list of arguments forwarded to
\code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}
}
\value{
A list with the selected pair features, the union of base genes
they reference, the step-1 intermediate set, per-iteration logs, and the
ridge coefficient matrix from the final step-2 pass.
}
\description{
Runs CPOP step 1 (iterated elastic-net selection with cohort intersection)
followed by CPOP step 2 (sign-agreement pruning using ridge refits) on the
pairwise log-ratio representation of the input. Features are returned as
\code{"A--B"} pair names produced by \code{\link[=pairwise_col_diff]{pairwise_col_diff()}}; for log-normalised
expression data these are mathematically equivalent to log-ratios.
}
\seealso{
\code{\link[=fit_cpop_panel]{fit_cpop_panel()}}, \code{\link[=select_transferable_features]{select_transferable_features()}}
}
