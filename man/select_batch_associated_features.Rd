% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_associated_features.R
\name{select_batch_associated_features}
\alias{select_batch_associated_features}
\title{Select Batch-Associated Features}
\usage{
select_batch_associated_features(
  x,
  cohort,
  y = NULL,
  n_features = 20L,
  n_pcs = 50L,
  batch_pvalue_threshold = 0.1,
  biology_pvalue_threshold = 0.2,
  biology_penalty_quantile = 0.5
)
}
\arguments{
\item{x}{Expression matrix (samples x genes). Should contain log-transformed
expression values.}

\item{cohort}{Factor of cohort/batch labels, one per row of \code{x}.}

\item{y}{Binary outcome labels for biology filtering. Set to \code{NULL} to skip
biology filtering (features selected purely on batch association).}

\item{n_features}{Number of batch-associated genes to select (default 20).}

\item{n_pcs}{Number of principal components to analyze (default 50).}

\item{batch_pvalue_threshold}{P-value threshold for batch association. PCs
with ANOVA p < this threshold are considered batch-associated (default
0.10).}

\item{biology_pvalue_threshold}{P-value threshold for biology non-association.
PCs with biology p > this threshold are considered biology-free (default
0.20).}

\item{biology_penalty_quantile}{Quantile of the per-gene biology scores used
to stabilise the batch/biology ratio (default \code{0.5}, the median). This acts
as a data-driven estimate of the biology-score noise floor; see Details.
Larger values weaken the biology penalty, with \code{1.0} reducing the ranking
to \code{batch_score} alone.}
}
\value{
A list with components:
\describe{
\item{features}{Character vector of selected batch-associated gene names.}
\item{biology_epsilon}{The stabilising constant actually added to the
biology scores when computing \code{ratio}.}
\item{diagnostics}{Data frame with per-PC statistics including variance
explained, batch p-value/R^2, and biology p-value/R^2.
}
\item{problematic_cohort}{The cohort most associated with batch effects.}
\item{one_vs_rest}{Data frame with per-cohort batch separation metrics.}
\item{pure_batch_pcs}{Integer vector of PC indices identified as pure batch.}
\item{gene_scores}{Data frame with per-gene batch and biology scores.}
\item{pca}{The \code{prcomp} object from PCA on input data.}
}
}
\description{
Identifies genes that are associated with batch/cohort effects but NOT with
the biological outcome, using PCA plus per-component ANOVA against supplied
cohort labels. The returned genes act as proxies for the batch axis: they
are useful as a confounding diagnostic in their own right, and as log-ratio
denominators, where \code{log(Signal) - log(BatchProxy)} cancels batch-specific
scaling factors (see \code{\link[=construct_dual_stream_ratios]{construct_dual_stream_ratios()}}).
}
\details{
The algorithm proceeds as follows:
\enumerate{
\item PCA on combined expression matrix
\item Test each PC for batch association (ANOVA) and biology association (if
\code{y} provided)
\item Identify "pure batch" PCs: batch-significant AND biology-nonsignificant
\item If no pure batch PCs found, fall back to LDA-based cohort discrimination
\item Extract genes with highest loadings on pure batch PCs
\item Score genes by ratio of batch to biology contribution
\item Return top \code{n_features} genes
}

Step 6 ranks by \code{batch_score / (biology_score + epsilon)}. Both scores are
variance-weighted means of absolute PCA loadings, so they share a scale that
depends on the number of genes and the data at hand. \code{epsilon} is therefore
set to the \code{biology_penalty_quantile} quantile of the observed biology
scores rather than a fixed constant: a fixed tiny epsilon lets genes whose
biology score is pure numerical noise (batch and biology both near zero)
generate arbitrarily large ratios and outrank genuinely batch-driven genes.
The median default assumes most genes are biologically null, making it a
robust estimate of the biology-score noise floor.

Unlike \code{\link[=select_ruv_features]{select_ruv_features()}}, which estimates a \emph{latent} unwanted-variation
subspace from negative control genes, this function requires explicit
cohort/batch labels and targets the batch axis directly.
}
\examples{
\dontrun{
# Assume x is samples x genes, cohort and y are factors
result <- select_batch_associated_features(
  x = expression_matrix,
  cohort = cohort_labels,
  y = disease_labels,
  n_features = 20
)

# Use the batch-proxy genes as denominators
ratios <- construct_dual_stream_ratios(
  x = expression_matrix,
  numerators = signal_genes,
  denominators = result$features
)
}

}
\seealso{
\code{\link[=construct_dual_stream_ratios]{construct_dual_stream_ratios()}} for using the selected genes as
ratio denominators, \code{\link[=select_ruv_features]{select_ruv_features()}} for the label-free alternative.
}
