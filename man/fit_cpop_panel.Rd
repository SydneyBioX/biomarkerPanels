% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpop_fit.R
\name{fit_cpop_panel}
\alias{fit_cpop_panel}
\title{Fit a CPOP Panel as a BiomarkerPanelResult}
\usage{
fit_cpop_panel(
  cpop_result,
  x_list,
  y_list,
  lambda_choice = c("lambda.min", "lambda.1se"),
  intercept = FALSE,
  assay = NULL,
  cv_control = list()
)
}
\arguments{
\item{cpop_result}{Output of \code{\link[=select_cpop_features]{select_cpop_features()}}.}

\item{x_list}{Training cohort matrices (same shape used for selection).}

\item{y_list}{Aligned binary response factors (\code{c("No", "Yes")}).}

\item{lambda_choice}{\code{"lambda.min"} (default) or \code{"lambda.1se"}.}

\item{intercept}{Logical; passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}. Default \code{FALSE}
matches CPOP's standard configuration where pair features are already
sample-centred via the ratio.}

\item{assay}{For \code{SummarizedExperiment} inputs, the assay name or index.}

\item{cv_control}{Optional named list forwarded to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}
}
\value{
A \linkS4class{BiomarkerPanelResult} with \code{feature_transform = "pairwise_ratios"} so that \code{\link[=evaluate_panel]{evaluate_panel()}} applies the same ratio
construction to validation data.
}
\description{
Implements CPOP step 3: refits ridge logistic regression on the selected
pair features for each cohort, then averages the per-cohort coefficient
vectors into a single linear model. At predict time this is identical to
averaging the per-cohort log-odds, which is how the original CPOP combines
its two cohort-specific models.
}
\details{
The returned \linkS4class{BiomarkerPanelResult} wraps the averaged coefficients
inside a \code{cv.glmnet} carrier so that \code{\link[=evaluate_panel]{evaluate_panel()}},
\code{\link[=evaluate_panel_by_cohort]{evaluate_panel_by_cohort()}}, and threshold tooling all compose with CPOP
panels without special-casing.
}
\seealso{
\code{\link[=select_cpop_features]{select_cpop_features()}}, \code{\link[=evaluate_panel]{evaluate_panel()}}
}
