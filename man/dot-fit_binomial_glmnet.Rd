% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_scoring.R
\name{.fit_binomial_glmnet}
\alias{.fit_binomial_glmnet}
\title{Fit a Cross-Validated Binomial Elastic Net}
\usage{
.fit_binomial_glmnet(x, y, alpha)
}
\arguments{
\item{x}{Numeric design matrix (samples x predictors), with cohort dummies
already appended if the caller uses them.}

\item{y}{Integer 0/1 response aligned with the rows of \code{x}.}

\item{alpha}{Elastic net mixing parameter (0 = ridge, 1 = lasso).}
}
\value{
A fitted \code{cv.glmnet} object.
}
\description{
Shared \code{cv.glmnet()} wrapper used by every inner-CV fit in the package
(fold scoring, in-sample regularized scoring, and the stored final model).
Keeping the call in one place holds the family, loss, and fold rule
identical across those sites: \code{nfolds} is \code{min(5, max(3, floor(n / 5)))},
so small panels never drop below 3 folds and large ones never exceed 5.
}
\details{
Errors propagate; callers needing a softer failure wrap the call in their
own \code{tryCatch()}.
}
\keyword{internal}
