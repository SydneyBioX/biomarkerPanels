% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_pareto_front}
\alias{plot_pareto_front}
\title{Plot Pareto Front on Held-Out Data}
\usage{
plot_pareto_front(
  optimization_result,
  x = NULL,
  y = NULL,
  cohort = NULL,
  objectives = NULL,
  x_metric = "sensitivity",
  y_metric = "specificity",
  color_by = "n_features",
  point_size = 4,
  point_alpha = 0.7,
  viridis_option = "plasma",
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  color_label = NULL,
  connect = FALSE,
  on_error = c("warn", "stop"),
  regularized = TRUE,
  verbose = interactive()
)
}
\arguments{
\item{optimization_result}{An \code{OptimizationResult} from \code{\link[=optimize_panel]{optimize_panel()}}.}

\item{x}{Held-out validation data: a matrix, data.frame,
\code{SummarizedExperiment}, or list of such objects. When \code{NULL} (default),
automatically uses held-out data stored by
\code{\link[=optimize_panel_transferable]{optimize_panel_transferable()}}.}

\item{y}{Held-out validation labels: a factor (or list of factors when \code{x}
is a list). When \code{NULL} (default), automatically uses held-out labels
stored by \code{\link[=optimize_panel_transferable]{optimize_panel_transferable()}}.}

\item{cohort}{Optional cohort factor for single-matrix \code{x}; passed to
\code{\link[=evaluate_pareto_solutions]{evaluate_pareto_solutions()}}.}

\item{objectives}{Optional objective override; passed to
\code{\link[=evaluate_pareto_solutions]{evaluate_pareto_solutions()}}.}

\item{x_metric}{Character; column name for the x-axis. Any column of
\code{\link[=evaluate_pareto_solutions]{evaluate_pareto_solutions()}} output, i.e. \code{"sensitivity"},
\code{"specificity"}, \code{"auc"}, \code{"n_features"}, \code{"n_base_features"}, or an
\code{obj_}-prefixed optimisation objective such as \code{"obj_auc"}. Default
\code{"sensitivity"}.}

\item{y_metric}{Character; column name for the y-axis. Same options as
\code{x_metric}. Default \code{"specificity"}.}

\item{color_by}{Column name to map to point colour (e.g. \code{"n_features"},
\code{"auc"}, an \code{obj_} column), or \code{NULL} to disable. Default
\code{"n_features"}.}

\item{point_size}{Numeric; point size. Default \code{4}.}

\item{point_alpha}{Numeric; point transparency. Default \code{0.7}.}

\item{viridis_option}{Character; viridis palette name. Default \code{"plasma"}.}

\item{title}{Character or \code{NULL}; plot title. Auto-generated when \code{NULL}.}

\item{xlab}{Character or \code{NULL}; x-axis label. Defaults to \code{x_metric}.}

\item{ylab}{Character or \code{NULL}; y-axis label. Defaults to \code{y_metric}.}

\item{color_label}{Character or \code{NULL}; color legend label. Defaults to
\code{color_by}.}

\item{connect}{Logical; if \code{TRUE}, connect points with a line sorted by the
x-axis metric. Default \code{FALSE}.}

\item{on_error}{One of \code{"warn"} or \code{"stop"}. Controls behaviour when a
solution fails evaluation. \code{"warn"} (default) skips the solution with a
warning; \code{"stop"} raises an error immediately.}

\item{regularized}{Logical; passed to \code{\link[=fit_panel]{fit_panel()}}. Default \code{TRUE}.}

\item{verbose}{Logical; print progress messages. Default \code{interactive()}.}
}
\value{
A \code{ggplot} object. The underlying metrics data frame is accessible
via \verb{$data}.
}
\description{
Fits and evaluates every Pareto-optimal solution from \code{\link[=optimize_panel]{optimize_panel()}} on
held-out validation data and displays the trade-off between two metrics as a
scatter plot. The underlying data frame is accessible via \verb{$data} on the
returned ggplot object.
}
\examples{
\dontrun{
opt <- optimize_panel(x_train, y_train, objectives = define_objectives())
p <- plot_pareto_front(opt, x_test, y_test)
p          # display plot
p$data     # access metrics data.frame
}
}
\seealso{
\code{\link[=evaluate_pareto_solutions]{evaluate_pareto_solutions()}}, \code{\link[=optimize_panel]{optimize_panel()}}, \code{\link[=fit_panel]{fit_panel()}},
\code{\link[=evaluate_panel]{evaluate_panel()}}
}
