% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_cohort.R
\name{metric_cohort_leakage}
\alias{metric_cohort_leakage}
\title{Cohort Leakage}
\usage{
metric_cohort_leakage(
  truth,
  scores = NULL,
  selected = NULL,
  positive = "Yes",
  cohort = NULL,
  ...
)
}
\arguments{
\item{truth}{Binary outcome; coerced with \code{\link[=ensure_binary_response]{ensure_binary_response()}}.}

\item{scores}{Numeric scores or probabilities.}

\item{selected}{Ignored; kept for signature compatibility.}

\item{positive}{Label treated as the positive ("event") class.}

\item{cohort}{Factor indicating cohort membership.}

\item{...}{Additional arguments (ignored).}
}
\value{
Maximum adjusted R-squared across classes, clamped to \code{[0, 1]}.
Returns 0 when \code{cohort} is \code{NULL} or contains a single level.
}
\description{
Measures how much of the within-class score variance is explained by cohort
membership, using adjusted R-squared from a one-way ANOVA
(\code{lm(scores ~ cohort)}). For each class (positives and negatives), the
adjusted R-squared is computed separately; the metric returns the maximum
across both classes.
}
\details{
A value near 0 indicates scores are independent of cohort within each class
(good transferability). A value near 1 indicates scores are almost entirely
determined by cohort (batch-effect leakage).
}
