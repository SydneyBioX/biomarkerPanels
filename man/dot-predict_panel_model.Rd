% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_prediction.R
\name{.predict_panel_model}
\alias{.predict_panel_model}
\title{Predict from a Stored Panel Model}
\usage{
.predict_panel_model(
  model,
  x_selected,
  cohort = NULL,
  expected_features = NULL
)
}
\arguments{
\item{model}{Fitted model: a \code{cv.glmnet}, an \code{npc} classifier, or a \code{glm}.}

\item{x_selected}{Numeric matrix of (already transformed) selected features.}

\item{cohort}{Optional cohort factor. Retained for signature compatibility
with the fitness paths; predictions are cohort-agnostic so it is unused.}

\item{expected_features}{Optional character vector of the transformed feature
names the model was trained on. When supplied (the evaluation paths), glm
prediction validates that the training and prediction feature names match
and reorders columns to the model's expectation. When \code{NULL} (the fitness
paths, where names match by construction) the check is skipped.}
}
\value{
Numeric vector of predicted probabilities, length \code{nrow(x_selected)}.
}
\description{
Dispatches on model type and returns predicted probabilities. Cohort-aware
models are scored cohort-agnostically (glmnet dummies zeroed, glm \code{.cohort}
set to its training reference level) because cohort-aware metrics split by
cohort downstream.
}
\keyword{internal}
