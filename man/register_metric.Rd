% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_registry.R
\name{register_metric}
\alias{register_metric}
\title{Register a metric function.}
\usage{
register_metric(
  name,
  fun,
  direction = c("maximize", "minimize"),
  label = name,
  cutoff_dependent = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{name}{Unique identifier for the metric.}

\item{fun}{Function implementing the metric. Must accept at least the arguments
\code{truth}, \code{scores}, and \code{selected}.}

\item{direction}{Either \code{"maximize"} or \code{"minimize"}.}

\item{label}{Human-readable name.}

\item{cutoff_dependent}{Logical; if \code{TRUE}, the metric depends on a probability
cutoff for classification (e.g., sensitivity, specificity). Cutoff-dependent
metrics may not behave as expected in \code{\link[=min_metric_constraint]{min_metric_constraint()}} because
their values depend on the arbitrary \code{cutoff_prob} parameter.}

\item{overwrite}{Logical; set to \code{TRUE} to replace an existing registration.}
}
\value{
Invisibly, the registered name.
}
\description{
Register a metric function.
}
