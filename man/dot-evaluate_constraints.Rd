% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness_eval.R
\name{.evaluate_constraints}
\alias{.evaluate_constraints}
\title{Evaluate Hard Constraints for a Candidate}
\usage{
.evaluate_constraints(
  constraint_specs,
  truth,
  scores,
  selected,
  cohort = NULL,
  x = NULL
)
}
\arguments{
\item{constraint_specs}{List of normalized constraint specs, each with a
\verb{$fun} predicate and a \verb{$label}.}

\item{truth}{Binary response factor.}

\item{scores}{Numeric predicted scores.}

\item{selected}{Base features selected for the candidate.}

\item{cohort}{Optional cohort factor.}

\item{x}{Optional (transformed) scoring matrix.}
}
\value{
A list with \code{results} (named logical vector) and \code{feasible} (scalar
logical).
}
\description{
Runs each constraint's predicate and reports both the per-constraint results
and whether the candidate is feasible (all constraints satisfied). Matches the
previous inline behavior exactly: an empty constraint set yields
\code{results = logical(0)} and \code{feasible = TRUE}.
}
\keyword{internal}
