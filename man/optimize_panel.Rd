% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_panel.R
\name{optimize_panel}
\alias{optimize_panel}
\title{Optimize Biomarker Panels with NSGA-III / NSGA-II}
\usage{
optimize_panel(
  x,
  y,
  objectives = define_objectives(metrics = c("sensitivity", "specificity",
    "num_features")),
  max_features = 5L,
  feature_pool = NULL,
  feature_transform = "pairwise_ratios",
  feature_alignment = c("intersection", "majority", "impute_median"),
  constraints = list(),
  algorithm = c("NSGA-III", "NSGA-II"),
  nsga_control = list(),
  assay = NULL,
  seed = NULL,
  fitness_mode = c("cv", "in_sample", "within_cohort_val", "within_cohort_rotating",
    "loco"),
  fitness_cv_folds = 5L,
  n_val_splits = 10L,
  train_ratio = 1,
  val_ratio = 0,
  np_alpha = 0.15,
  np_delta = 0.05,
  regularized = TRUE,
  regularized_alpha = 0.5,
  selection_threshold = "adaptive",
  record_history = FALSE,
  fitness_cv = NULL
)
}
\arguments{
\item{x}{Matrix-like object, \code{SummarizedExperiment}, or list of matrices /
experiments representing one or more cohorts.}

\item{y}{Binary response (\code{factor}, \code{character}, or \code{logical}) aligned with
\code{x}. When \code{x} is a list, \code{y} must be a list of the same length.}

\item{objectives}{Named list of objective descriptors as returned by
\code{\link[=define_objectives]{define_objectives()}}.}

\item{max_features}{Maximum number of biomarkers permitted in a panel. Acts
as an upper bound; actual panel size varies based on how many features have
weight > 0.5 in the decision vector. Minimum is 2 features when
\code{regularized = TRUE} (glmnet requirement), or 1 when \code{regularized = FALSE}.}

\item{feature_pool}{Optional subset of base feature identifiers (names or
integer indices) to consider during optimization. These are the original
feature names before any transformation. When using pairwise transforms,
transformed labels like \code{"A--B"} are also accepted and will be mapped
back to their constituent base features. Defaults to all features.}

\item{feature_transform}{Transformation applied to selected features
after base feature selection. The NSGA algorithm selects base features,
then this transform is applied on-the-fly. Defaults to \code{"pairwise_ratios"},
which generates pairwise contrasts via \code{\link[=pairwise_col_diff]{pairwise_col_diff()}} to dampen
batch effects. Other options include \code{"pairwise_log_ratios"} (log-ratios
for compositional data), \code{"reference_norm"} (normalize relative to a
reference feature), and \code{"none"} (no transformation). Use
\code{\link[=register_feature_transform]{register_feature_transform()}} to add custom strategies. See
\code{\link[=feature_transform_registry]{feature_transform_registry()}} for available options.}

\item{feature_alignment}{Strategy for aligning features across cohorts:
\describe{
\item{"intersection"}{(Default) Keep only features present in ALL cohorts.
Features missing in any cohort are dropped.}
\item{"majority"}{Keep features present in >= 50\% of cohorts.
Missing values are imputed with cohort medians.}
\item{"impute_median"}{Keep all features from any cohort.
Missing values are imputed with cohort medians.}
}}

\item{constraints}{Optional list of constraint descriptors (e.g.,
from \code{\link[=min_metric_constraint]{min_metric_constraint()}}) that must evaluate to \code{TRUE} for a candidate
solution to be considered feasible.}

\item{algorithm}{Multi-objective optimization algorithm. \code{"NSGA-III"}
(default) uses reference-point-based selection and gives better Pareto
front diversity for many-objective problems (3+ objectives). \code{"NSGA-II"}
uses crowding distance for diversity preservation and is typically
adequate for 2 objectives.}

\item{nsga_control}{Named list of arguments passed to \code{\link[rmoo:nsga3]{rmoo::nsga3()}} (or
\code{\link[rmoo:nsga2]{rmoo::nsga2()}}). For NSGA-III, \code{n_partitions} controls reference point
density (default computed from number of objectives). Defaults to adaptive
values based on feature pool size. Note: \code{parallel = TRUE} is not recommended
as it is slower than sequential execution due to overhead.}

\item{assay}{For \code{SummarizedExperiment} inputs, assay name or index to use.}

\item{seed}{Optional integer seed for reproducibility. When provided, the
seed is set once before partitioning, so both the data split and the NSGA
search are reproducible. If \code{NULL} (default), no seed is set and results
may vary between runs.}

\item{fitness_mode}{How candidate panels are scored during the NSGA search:
\describe{
\item{"cv"}{(Default) k-fold cross-validation within the training
partition; objectives are computed on out-of-fold predictions.}
\item{"in_sample"}{Train and score on the same samples. Fast but
overfits; kept for small datasets and backward compatibility.}
\item{"within_cohort_val"}{Train on the training partition, score on the
validation partition. Requires \code{val_ratio > 0}.}
\item{"within_cohort_rotating"}{Pool train and validation, pre-compute
\code{n_val_splits} stratified splits, and rotate to the next split every
generation so no single split can be overfit to. Requires
\code{val_ratio > 0}.}
\item{"loco"}{Leave-one-cohort-out: score each candidate on concatenated
out-of-cohort predictions, making cross-cohort transferability the
objective. Requires at least 2 cohorts.}
}}

\item{fitness_cv_folds}{Number of cross-validation folds for fitness
evaluation when \code{fitness_mode = "cv"}. Default is 5.}

\item{n_val_splits}{Number of rotating train/validation splits to
pre-compute when \code{fitness_mode = "within_cohort_rotating"} (default 10).}

\item{train_ratio, val_ratio}{Per-cohort shares used for stratified
partitioning; the remainder (\code{1 - train_ratio - val_ratio}) is held out and
stored on the result for \code{\link[=calibrate_panel]{calibrate_panel()}}. The default
\code{train_ratio = 1}, \code{val_ratio = 0} means no partitioning at all: the whole
input is the training data.}

\item{np_alpha}{Type I error rate for Neyman-Pearson threshold selection.
Stored on the result for \code{\link[=calibrate_panel]{calibrate_panel()}} when a held-out partition
exists (default 0.15).}

\item{np_delta}{Tolerance for Neyman-Pearson threshold selection, stored
alongside \code{np_alpha} (default 0.05).}

\item{regularized}{Logical; if \code{TRUE} (default), use regularized regression
(elastic net via glmnet) for scoring candidates during optimization. This
typically produces more stable fitness estimates and better out-of-sample
performance compared to unregularized logistic regression.}

\item{regularized_alpha}{Elastic net mixing parameter when \code{regularized = TRUE}.
\code{alpha = 1} is lasso, \code{alpha = 0} is ridge, and values in between give
elastic net. Default is 0.5 (elastic net).}

\item{selection_threshold}{Either a fixed numeric threshold in (0,1) for
feature selection, or \code{"adaptive"} (default) to use gap-based selection
that allows variable panel sizes in the Pareto front. Fixed thresholds
(e.g., 0.5) tend to produce panels at \code{max_features} because more features
improve accuracy. Adaptive selection finds natural breakpoints in the
weight distribution, enabling the \code{num_features} objective to drive panel
size diversity.}

\item{record_history}{Logical; if \code{TRUE}, capture the population, fitness,
and NSGA rank at every generation and attach the result to the returned
\code{OptimizationResult}. Retrieve via \code{\link[=nsga_history]{nsga_history()}}. Default \code{FALSE}.
Adds modest overhead (one matrix copy per generation) and is intended for
diagnostic / visualization use, e.g. plotting how the Pareto front
evolves over \code{maxiter}.}

\item{fitness_cv}{Deprecated. Logical alias for \code{fitness_mode}: \code{TRUE} maps
to \code{"cv"} and \code{FALSE} to \code{"in_sample"}. Supplying it emits a deprecation
warning and overrides \code{fitness_mode}.}
}
\value{
An \code{OptimizationResult} containing the Pareto-optimal solutions.
Use \code{\link[=summarize_solutions]{summarize_solutions()}} to inspect solutions and \code{\link[=fit_panel]{fit_panel()}} to
fit a model on a selected solution.
}
\description{
Wrapper around \code{\link[rmoo:nsga3]{rmoo::nsga3()}} (or \code{\link[rmoo:nsga2]{rmoo::nsga2()}}) that composes registered
metric functions into a multi-objective search for compact biomarker panels.
Candidate solutions are represented as weights over a feature pool; features
with weight > 0.5 are included in the panel (up to \code{max_features}). This
threshold-based selection allows variable panel sizes, making the
\code{num_features} objective meaningful. Inputs may be a single cohort (\code{matrix},
\code{data.frame}, or \code{SummarizedExperiment}) or multiple cohorts supplied as
lists of such objects. Optimisation should be run on training data only---use
\code{\link[=evaluate_panel]{evaluate_panel()}} for held-out validation.
}
\section{Partitioning}{

When \code{train_ratio + val_ratio < 1}, each cohort is stratified into
train / validation / held-out shares before the search runs; \code{fitness_mode}
determines which of these partitions
the NSGA search actually scores against (see above). The returned
\code{OptimizationResult} always carries the pooled train + validation data as
\code{aggregated_x}/\code{aggregated_y}/\code{aggregated_cohort}. The held-out partition,
\code{np_alpha}, \code{np_delta}, and \code{partition_info} are stored on \code{control} only
when a positive held-out share exists (\code{train_ratio + val_ratio < 1}); with
the default \verb{train_ratio = 1, val_ratio = 0} no partitioning happens at all
and those fields are absent. \code{\link[=calibrate_panel]{calibrate_panel()}} and
\code{\link[=evaluate_pareto_solutions]{evaluate_pareto_solutions()}} read \code{control$heldout_x} /
\code{control$heldout_y} / \code{control$heldout_cohort} when present.
}

\seealso{
\code{\link[=fit_panel]{fit_panel()}}, \code{\link[=summarize_solutions]{summarize_solutions()}}, \code{\link[=evaluate_panel]{evaluate_panel()}}
}
