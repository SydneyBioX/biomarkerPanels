% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_panel_binary.R
\name{optimize_panel_binary}
\alias{optimize_panel_binary}
\title{Optimize Biomarker Panels with Binary Genetic Algorithm}
\usage{
optimize_panel_binary(
  x,
  y,
  loss = "auc",
  max_features = 5L,
  min_features = 2L,
  feature_pool = NULL,
  cohort_aggregator = "pairwise_ratios",
  feature_alignment = c("intersection", "majority", "impute_median"),
  ga_control = list(),
  assay = NULL,
  seed = NULL,
  fitness_cv = TRUE,
  fitness_cv_folds = 5L,
  regularized_alpha = 0.5,
  parallel = FALSE
)
}
\arguments{
\item{x}{Matrix-like object, \code{SummarizedExperiment}, or list of matrices /
experiments representing one or more cohorts.}

\item{y}{Binary response (\code{factor}, \code{character}, or \code{logical}) aligned with
\code{x}. When \code{x} is a list, \code{y} must be a list of the same length.}

\item{loss}{Primary loss function to optimize (default: "auc").}

\item{max_features}{Maximum number of biomarkers permitted in a panel.}

\item{min_features}{Minimum number of biomarkers required. Default 2.}

\item{feature_pool}{Optional subset of feature identifiers to consider.}

\item{cohort_aggregator}{Transformation applied to cohort feature matrices.
Defaults to \code{"pairwise_ratios"}.}

\item{feature_alignment}{Strategy for aligning features across cohorts.}

\item{ga_control}{Named list of arguments passed to \code{\link[GA:ga]{GA::ga()}}. Defaults
to \code{list(popSize = 100, maxiter = 100, pmutation = 0.1, pcrossover = 0.8)}.}

\item{assay}{For \code{SummarizedExperiment} inputs, assay name or index to use.}

\item{seed}{Optional integer seed for reproducibility.}

\item{fitness_cv}{Logical; if \code{TRUE} (default), use cross-validation when
evaluating candidate solutions.}

\item{fitness_cv_folds}{Number of cross-validation folds for fitness
evaluation when \code{fitness_cv = TRUE}. Default is 5.}

\item{regularized_alpha}{Elastic net mixing parameter. Default is 0.5.}

\item{parallel}{Whether to run fitness evaluations in parallel. Default FALSE.}
}
\value{
A \code{BiomarkerPanelResult} with the best solution found.
}
\description{
Alternative to \code{\link[=optimize_panel]{optimize_panel()}} that uses binary encoding for feature
selection. Each chromosome is a binary vector where 1 indicates feature
inclusion and 0 indicates exclusion. This eliminates the continuous-to-
discrete mapping problem of the standard NSGA-II approach.
}
