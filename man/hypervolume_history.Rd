% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypervolume.R
\name{hypervolume_history}
\alias{hypervolume_history}
\alias{hypervolume_history,OptimizationResult-method}
\title{Hypervolume Trajectory Across NSGA Generations}
\usage{
hypervolume_history(
  object,
  ref = NULL,
  ref_buffer = 0.05,
  include_dominated = FALSE
)

\S4method{hypervolume_history}{OptimizationResult}(
  object,
  ref = NULL,
  ref_buffer = 0.05,
  include_dominated = FALSE
)
}
\arguments{
\item{object}{An \code{OptimizationResult} with non-empty \code{nsga_history()}.}

\item{ref}{Optional numeric reference point in user-facing direction. If
\code{NULL}, derived from the final generation's Pareto set.}

\item{ref_buffer}{Fractional outward shift applied per objective when \code{ref}
is auto-computed. Default \code{0.05}.}

\item{include_dominated}{Logical; if \code{TRUE}, compute HV over the full
generation population rather than only the rank-1 individuals. Default
\code{FALSE}.}
}
\value{
A data frame with columns \code{generation} and \code{hypervolume}. The
reference point used is attached as \code{attr(., "ref")}.
}
\description{
Compute the dominated hypervolume of the rank-1 (Pareto) population at each
recorded generation. Useful for diagnosing convergence — a flattening curve
suggests further iterations are unlikely to expand the front.
}
\details{
Requires the optimization to have been run with \code{record_history = TRUE} and
the suggested \code{emoa} package to be installed. The reference point is held
fixed across generations so HV values are comparable: if not supplied, the
nadir of the \emph{final} generation's Pareto set is used (with \code{ref_buffer}).
}
\section{Methods (by class)}{
\itemize{
\item \code{hypervolume_history(OptimizationResult)}: HV trajectory by generation.

}}
