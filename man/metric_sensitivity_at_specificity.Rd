% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_discrimination.R
\name{metric_sensitivity_at_specificity}
\alias{metric_sensitivity_at_specificity}
\title{Sensitivity at a Fixed Specificity Threshold}
\usage{
metric_sensitivity_at_specificity(
  truth,
  scores = NULL,
  selected = NULL,
  positive = "Yes",
  target_specificity = 0.9
)
}
\arguments{
\item{truth}{Binary outcome; coerced with \code{\link[=ensure_binary_response]{ensure_binary_response()}}.}

\item{scores}{Numeric scores or probabilities.}

\item{selected}{Ignored; kept for signature compatibility.}

\item{positive}{Label treated as the positive ("event") class.}

\item{target_specificity}{The specificity threshold at which to evaluate
sensitivity (default 0.90). Must be between 0 and 1.}
}
\value{
Sensitivity at the target specificity, between 0 and 1. Returns \code{0}
for degenerate inputs.
}
\description{
Computes the sensitivity achievable when the classifier operates at a
specified specificity level. This is the transpose of
\code{\link[=metric_specificity_at_sensitivity]{metric_specificity_at_sensitivity()}}, and is the natural objective for
"rule-in" applications where a minimum specificity (i.e. a tolerable false
positive rate) is mandatory and we want the best achievable detection rate
at that operating point.
}
\details{
Uses \code{\link[pROC:coords]{pROC::coords()}} to find the operating point on the ROC curve
corresponding to the target specificity, then returns the sensitivity at
that point. If the exact target specificity cannot be achieved, pROC
interpolates to the nearest achievable point.

Unlike \code{\link[=metric_specificity_at_sensitivity]{metric_specificity_at_sensitivity()}}, degenerate inputs (a
single-class subset, or a \code{coords()} call that yields nothing usable)
return \code{0} — the worst achievable value — rather than erroring or returning
\code{NA_real_}. This keeps the metric safe to use directly as an NSGA
objective, since the fitness path does not guard against \code{NA} objective
values. Argument errors (missing \code{scores}, out-of-range
\code{target_specificity}) still signal an error.
}
\examples{
truth <- factor(c(rep("No", 50), rep("Yes", 50)), levels = c("No", "Yes"))
scores <- c(runif(50, 0, 0.6), runif(50, 0.4, 1))

# Sensitivity when specificity is fixed at 90\%
metric_sensitivity_at_specificity(truth, scores, target_specificity = 0.90)

# Sensitivity when specificity is fixed at 95\%
metric_sensitivity_at_specificity(truth, scores, target_specificity = 0.95)
}
\seealso{
\code{\link[=metric_specificity_at_sensitivity]{metric_specificity_at_sensitivity()}} for the transpose,
\code{\link[=metric_pauc]{metric_pauc()}} for partial AUC in the high-sensitivity region,
\code{\link[=metric_sensitivity]{metric_sensitivity()}} and \code{\link[=metric_specificity]{metric_specificity()}} for threshold-based metrics.
}
\keyword{internal}
