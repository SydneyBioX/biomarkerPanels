% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discriminative_features.R
\name{select_discriminative_features}
\alias{select_discriminative_features}
\title{Select Discriminative Features with Cohort Penalty}
\usage{
select_discriminative_features(
  x_list,
  y_list,
  n_features = 50L,
  lambda_cohort = 1,
  lambda_sd = 1,
  min_auc = 0.6,
  assay = NULL
)
}
\arguments{
\item{x_list}{A matrix-like object, \code{SummarizedExperiment}, or list of such
objects. Rows represent samples and columns represent features.}

\item{y_list}{A binary response aligned with \code{x_list}. Must be a factor with
levels \code{c("No", "Yes")}. Provide a list when \code{x_list} is a list.}

\item{n_features}{Maximum number of features to return (default \code{50}).}

\item{lambda_cohort}{Penalty weight for cohort discrimination (default \code{1.0}).
Higher values more aggressively penalise features that separate cohorts.}

\item{lambda_sd}{Penalty weight for cross-cohort AUC variability (default
\code{1.0}). Higher values favour features with consistent AUC across cohorts.}

\item{min_auc}{Minimum mean within-cohort AUC required (default \code{0.6}).
Features below this threshold are excluded before ranking.}

\item{assay}{For \code{SummarizedExperiment} inputs, the assay name or index to
extract prior to analysis.}
}
\value{
A list with components:
\describe{
\item{features}{Character vector of selected feature names.}
\item{scores}{Data frame with per-feature scoring metadata: \code{feature},
\code{mean_auc}, \code{sd_auc}, \code{min_auc}, \code{cohort_disc}, \code{score}.}
\item{per_cohort_auc}{Matrix of within-cohort AUC values (features x
cohorts).}
\item{settings}{List of parameter values used.}
}
}
\description{
Ranks features by a composite score that rewards within-cohort discrimination
(outcome AUC) while penalising features that separate cohorts (batch
confounding) or have inconsistent performance across cohorts (AUC variance).
}
\details{
The composite score for feature \eqn{j} is:
\deqn{score_j = \bar{AUC}_j - \lambda_1 \cdot cohort\_disc_j - \lambda_2 \cdot sd(AUC_j)}

where \eqn{\bar{AUC}_j} is the mean within-cohort AUC, \eqn{cohort\_disc_j}
measures how well feature \eqn{j} separates cohorts (irrespective of
outcome), and \eqn{sd(AUC_j)} captures cross-cohort variability.
}
