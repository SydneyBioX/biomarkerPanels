% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_cohort_comparison}
\alias{plot_cohort_comparison}
\title{Plot Per-Cohort Metric Comparison}
\usage{
plot_cohort_comparison(
  cohort_metrics,
  metrics = NULL,
  facet_by = c("metric", "cohort"),
  show_values = TRUE,
  value_size = 3,
  y_limits = c(0, 1),
  title = "Per-Cohort Performance Comparison",
  subtitle = NULL
)
}
\arguments{
\item{cohort_metrics}{A \code{data.frame} from \code{\link[=evaluate_panel_by_cohort]{evaluate_panel_by_cohort()}}, or
any data frame with a \code{cohort} column and one or more metric columns.}

\item{metrics}{Character vector specifying which metrics to plot. Defaults
to all numeric columns except count columns.}

\item{facet_by}{One of \code{"metric"} (facet by metric, x-axis = cohort) or
\code{"cohort"} (facet by cohort, x-axis = metric). Defaults to \code{"metric"}.}

\item{show_values}{Logical; if \code{TRUE}, display metric values as text labels
on bars. Defaults to \code{TRUE}.}

\item{value_size}{Numeric; text size for value labels. Defaults to \code{3}.}

\item{y_limits}{Numeric vector of length 2 specifying y-axis limits, or
\code{NULL} for automatic limits. Defaults to \code{c(0, 1)}.}

\item{title}{Plot title. Defaults to \code{"Per-Cohort Performance Comparison"}.}

\item{subtitle}{Optional plot subtitle.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a grouped bar chart comparing performance metrics across cohorts.
When a \code{method} column is present, bars are dodged by method for comparing
multiple approaches (e.g., MOO vs LASSO).
}
\examples{
# cohort_metrics <- evaluate_panel_by_cohort(panel, x_list, y_list)
# plot_cohort_comparison(cohort_metrics)
#
# Compare methods:
# moo_metrics$method <- "MOO"
# lasso_metrics$method <- "LASSO"
# plot_cohort_comparison(rbind(moo_metrics, lasso_metrics))
}
\seealso{
\code{\link[=evaluate_panel_by_cohort]{evaluate_panel_by_cohort()}}
}
