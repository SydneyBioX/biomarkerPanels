% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_summarization.R
\name{select_panel_top_sensitivity}
\alias{select_panel_top_sensitivity}
\title{Select the top-sensitivity panel at 90\% specificity.}
\usage{
select_panel_top_sensitivity(
  performance,
  solution_col = "solution_id",
  external_col = "external_sens_at_spec90",
  internal_col = "internal_sens_at_spec90",
  max_loss = Inf,
  panel_size_col = NULL
)
}
\arguments{
\item{performance}{Data frame where each row summarises a solution. Must
contain at minimum columns referenced by \code{solution_col}, \code{external_col},
and \code{internal_col}.}

\item{solution_col}{Column name holding the \code{solution_id}.}

\item{external_col}{Column name holding sensitivity at 90\% specificity for
the external validation cohort.}

\item{internal_col}{Column name holding the corresponding internal
cross-validation sensitivity.}

\item{max_loss}{Maximum allowable drop in sensitivity between internal and
external evaluations (\code{internal - external}). Defaults to \code{Inf}.}

\item{panel_size_col}{Optional column containing the number of biomarkers in
each solution. Used purely as a tie-breaker (smaller panels preferred).}
}
\value{
A single-row data frame describing the selected solution. An extra
column named \code{performance_loss} (internal minus external sensitivity) is
appended for convenience. Returns \code{NULL} if no solution satisfies
\code{max_loss} or if required columns are missing.
}
\description{
Given solution-level performance summaries this function identifies the panel
that maximises external sensitivity while constraining the loss relative to
internal cross-validation performance.
}
