% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_summarization.R
\name{analyze_feature_stability}
\alias{analyze_feature_stability}
\title{Analyze Feature Selection Stability Across Pareto Solutions}
\usage{
analyze_feature_stability(
  result,
  feature_type = c("features", "base_features")
)
}
\arguments{
\item{result}{An \code{\link{OptimizationResult}} or \code{\link{BiomarkerPanelResult}}.}

\item{feature_type}{One of \code{"features"} (default) or \code{"base_features"}.
When \code{"base_features"}, counts original gene names rather than transformed
feature names (e.g. pairwise ratios). Only supported for
\code{OptimizationResult} input.}
}
\value{
A list with class \code{"FeatureStabilityResult"} containing:
\describe{
\item{frequencies}{Data frame with columns \code{feature}, \code{count}, \code{proportion}
ordered by descending count.}
\item{jaccard_matrix}{Symmetric matrix of pairwise Jaccard similarity
coefficients between solutions. Row/column names are \code{solution_id} values.}
\item{n_solutions}{Integer count of Pareto-optimal solutions analyzed.}
\item{mean_jaccard}{Mean pairwise Jaccard similarity (excluding diagonal).}
}
}
\description{
Computes feature inclusion frequencies and pairwise Jaccard similarity
between all Pareto-optimal solutions. Accepts either an
\code{\link{OptimizationResult}} (preferred — contains all Pareto solutions) or a
\code{\link{BiomarkerPanelResult}} (legacy path).
}
\examples{
\dontrun{
# From an OptimizationResult (preferred):
stability <- analyze_feature_stability(opt_result)
stability$frequencies
plot_feature_stability(stability)

# Gene-level frequencies with pairwise transforms:
stability_genes <- analyze_feature_stability(opt_result,
                                             feature_type = "base_features")
}
}
\seealso{
\code{\link[=compute_inclusion_frequencies]{compute_inclusion_frequencies()}}, \code{\link[=plot_feature_stability]{plot_feature_stability()}}
}
