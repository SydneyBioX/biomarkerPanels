% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness_eval.R
\name{.make_fitness_scaffold}
\alias{.make_fitness_scaffold}
\title{Build the Shared Fitness Scaffold}
\usage{
.make_fitness_scaffold(
  feature_pool,
  max_features,
  min_features_required,
  selection_threshold,
  matrices,
  feature_transform,
  objectives,
  constraints,
  cache_max_entries = Inf
)
}
\arguments{
\item{feature_pool}{Character vector of base feature names.}

\item{max_features}{Maximum base features to include.}

\item{min_features_required}{Minimum number of base features.}

\item{selection_threshold}{Either \code{"adaptive"} or a fixed numeric threshold.}

\item{matrices}{Named list of raw base-feature matrices handed to
\code{.make_panel_transformer()}.}

\item{feature_transform}{Name of the feature transform to apply.}

\item{objectives}{Objective list from \code{\link[=define_objectives]{define_objectives()}}.}

\item{constraints}{Constraint list.}

\item{cache_max_entries}{Maximum entries retained per cache.}
}
\value{
List with \code{selector}, \code{transform}, \code{cache}, \code{directions},
\code{constraint_specs}, and \code{finalize(evaluate_candidate, cache_fitness)},
which wraps a scoring body into the \code{list(wrapper, evaluate)} contract the
optimizers consume.
}
\description{
Every fitness implementation (the inline \code{\link[=optimize_panel]{optimize_panel()}} fitness and the
validation / rotating-validation / LOCO factories) shares the same skeleton:
a panel selector, an on-the-fly panel transformer, an objective cache, and
the candidate-to-objective conversion handed to rmoo. This factory owns that
skeleton; each fitness strategy supplies only its \code{evaluate_candidate}
scoring body.
}
\keyword{internal}
