% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_cohort_transferability.R
\name{metric_conditional_score_shift}
\alias{metric_conditional_score_shift}
\title{Class-Conditional Score Shift}
\usage{
metric_conditional_score_shift(
  truth,
  scores = NULL,
  selected = NULL,
  positive = "Yes",
  cohort = NULL,
  ...
)
}
\arguments{
\item{truth}{Binary outcome; coerced with \code{\link[=ensure_binary_response]{ensure_binary_response()}}.}

\item{scores}{Numeric scores or probabilities.}

\item{selected}{Ignored; kept for signature compatibility.}

\item{positive}{Label treated as the positive ("event") class.}

\item{cohort}{Factor indicating cohort membership.}

\item{...}{Additional arguments (ignored).}
}
\value{
Maximum pairwise W1 distance across classes, or 0 when \code{cohort}
is \code{NULL} or contains a single level. Returns \code{NA_real_} when all
cohort subgroups are too small (< 10 samples) for reliable comparison.
}
\description{
Measures the maximum pairwise Wasserstein (W1) distance between cohort-level
score distributions within each class. For each class, all cohort pairs are
compared; the metric returns the worst-case shift across both classes.
}
\details{
Unlike \code{\link{metric_cohort_leakage}}, this metric is fully
non-parametric and captures any distributional shift (location, scale, or
shape), not only mean differences.
}
