% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_discrimination.R
\name{metric_specificity_at_sensitivity}
\alias{metric_specificity_at_sensitivity}
\title{Specificity at a Fixed Sensitivity Threshold}
\usage{
metric_specificity_at_sensitivity(
  truth,
  scores = NULL,
  selected = NULL,
  positive = "Yes",
  target_sensitivity = 0.9
)
}
\arguments{
\item{truth}{Binary outcome; coerced with \code{\link[=ensure_binary_response]{ensure_binary_response()}}.}

\item{scores}{Numeric scores or probabilities.}

\item{selected}{Ignored; kept for signature compatibility.}

\item{positive}{Label treated as the positive ("event") class.}

\item{target_sensitivity}{The sensitivity threshold at which to evaluate
specificity (default 0.90). Must be between 0 and 1.}
}
\value{
Specificity at the target sensitivity, between 0 and 1. Returns \code{0}
for degenerate inputs.
}
\description{
Computes the specificity achievable when the classifier operates at a
specified sensitivity level. This is useful for rule-out diagnostic
applications where a minimum sensitivity is mandatory, and we want to
know the best achievable specificity at that operating point.
}
\details{
Uses \code{\link[pROC:coords]{pROC::coords()}} to find the operating point on the ROC curve
corresponding to the target sensitivity, then returns the specificity
at that point. If the exact target sensitivity cannot be achieved,
pROC interpolates to the nearest achievable point.

Degenerate inputs (a single-class subset, or a \code{coords()} call that yields
nothing usable) return \code{0} — the worst achievable value — rather than
erroring or returning \code{NA_real_}. This keeps the metric safe to use
directly as an NSGA objective: an error would abort the whole run, and an
\code{NA} is mapped to the \code{1e6} infeasibility penalty, which conflates
degeneracy with constraint violation, distorts NSGA-III normalization, and
still surfaces as \code{NA} in post-hoc solution re-evaluation. Argument errors
(missing \code{scores}, out-of-range \code{target_sensitivity}) still signal an
error.
}
\examples{
truth <- factor(c(rep("No", 50), rep("Yes", 50)), levels = c("No", "Yes"))
scores <- c(runif(50, 0, 0.6), runif(50, 0.4, 1))

# Specificity when sensitivity is fixed at 90\%
metric_specificity_at_sensitivity(truth, scores, target_sensitivity = 0.90)

# Specificity when sensitivity is fixed at 95\%
metric_specificity_at_sensitivity(truth, scores, target_sensitivity = 0.95)
}
\seealso{
\code{metric_pauc()} for partial AUC in the high-sensitivity region,
\code{metric_sensitivity()} and \code{metric_specificity()} for threshold-based
metrics. All three are internal; reach them by registry name via
\code{\link[=define_objectives]{define_objectives()}}.
}
