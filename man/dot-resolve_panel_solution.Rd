% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{.resolve_panel_solution}
\alias{.resolve_panel_solution}
\title{Resolve a Pareto Solution into Training-Ready Inputs}
\usage{
.resolve_panel_solution(
  optimization_result,
  solution_id = NULL,
  features = NULL,
  x = NULL,
  y = NULL,
  cohort = NULL
)
}
\arguments{
\item{optimization_result}{An \code{OptimizationResult}.}

\item{solution_id}{Optional solution ID; auto-selected when \code{NULL}.}

\item{features}{Optional explicit base features (overrides \code{solution_id}).}

\item{x, y, cohort}{Optional training data; defaults to the aggregated data
stored on \code{optimization_result}.}
}
\value{
List with \code{base_features}, \code{features} (transformed names),
\code{solution_id} (\code{NA} for explicit features), \code{metrics} (named numeric or
\code{NULL}), \code{x_raw}, \code{x_selected} (transform applied), \code{truth}, and \code{cohort}.
}
\description{
Shared front half of \code{\link[=fit_panel]{fit_panel()}} and \code{\link[=fit_np_panel]{fit_np_panel()}}: chooses the
solution (explicit \code{features}, explicit \code{solution_id}, or auto-select on
the first objective), resolves training data from arguments or the stored
slots, validates the base features, and applies the stored feature
transform.
}
\keyword{internal}
