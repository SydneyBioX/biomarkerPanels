% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_pareto_evolution}
\alias{plot_pareto_evolution}
\title{Animate Pareto Front Evolution Alongside Feature Inclusion}
\usage{
plot_pareto_evolution(
  optimization_result,
  x_metric = "sensitivity",
  y_metric = "specificity",
  top_n_features = 15L,
  generations = NULL,
  fps = 4,
  width = 1000,
  height = 500,
  point_size = 3,
  viridis_option = "plasma",
  path = NULL
)
}
\arguments{
\item{optimization_result}{An \code{OptimizationResult} from \code{\link[=optimize_panel]{optimize_panel()}}.}

\item{x_metric, y_metric}{Objective names for the Pareto panel axes.
Defaults \code{"sensitivity"} / \code{"specificity"}.}

\item{top_n_features}{Integer; number of features to display on the
right panel. Default \code{15}.}

\item{generations}{One of: \code{NULL} (all recorded generations, default), a
single integer \code{n} giving the number of evenly-spaced frames to render,
or an integer vector of specific generation numbers.}

\item{fps}{Numeric; animation frames per second. Default \code{4}.}

\item{width, height}{Frame dimensions in pixels. Default \code{1000} x \code{500}.}

\item{point_size}{Point size in the Pareto panel. Default \code{3}.}

\item{viridis_option}{Viridis palette for coloring by panel size. Default
\code{"plasma"}.}

\item{path}{Optional file path to write the GIF to. Default \code{NULL}
(in-memory only).}
}
\value{
A \code{magick-image} representing the animated GIF.
}
\description{
Two-panel animated GIF showing how the rank-1 (Pareto) population evolves
across NSGA generations: the left panel plots the Pareto front in
objective space, the right panel plots inclusion frequency of base
features in that generation's Pareto solutions. Both axes (Pareto-panel
x/y, frequency-panel features) are held fixed across frames so the
animation is comparable frame-to-frame.
}
\details{
Requires the optimization to have been run with \code{record_history = TRUE}.
The set of features shown on the right panel is the top-N base features
by total appearances across the rank-1 population of all recorded
generations; features not present in a given generation drop to zero.
}
\examples{
\dontrun{
opt <- optimize_panel(x, y, record_history = TRUE)
anim <- plot_pareto_evolution(opt)
anim  # display in RStudio viewer
plot_pareto_evolution(opt, path = "evolution.gif")
}
}
\seealso{
\code{\link[=optimize_panel]{optimize_panel()}}, \code{\link[=nsga_history]{nsga_history()}}, \code{\link[=hypervolume_history]{hypervolume_history()}}
}
