% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_inputs.R
\name{.prepare_selection_inputs}
\alias{.prepare_selection_inputs}
\title{Prepare Cohort Inputs for Feature Selection}
\usage{
.prepare_selection_inputs(
  x_list,
  y_list,
  assay = NULL,
  response = c("integer", "factor"),
  align = c("intersection", "union"),
  feature_order = c("input", "sorted")
)
}
\arguments{
\item{x_list}{A matrix, \code{SummarizedExperiment}, or list of such objects.}

\item{y_list}{A binary response aligned with \code{x_list}.}

\item{assay}{For \code{SummarizedExperiment} inputs, the assay name or index.}

\item{response}{Response encoding to return: \code{"integer"} for 0/1 (the form
\code{glmnet} and RUV want) or \code{"factor"} for the standard \code{No}/\code{Yes} factor.}

\item{align}{\code{"intersection"} keeps only features shared by every cohort and
subsets each matrix to them. \code{"union"} keeps every feature seen in any
cohort and leaves the matrices ragged — the caller is then responsible for
handling features absent from a given cohort (limma fits per cohort and
pads the resulting statistic matrix with \code{NA}).}

\item{feature_order}{\code{"input"} preserves the column order of the first
cohort; \code{"sorted"} sorts feature names alphabetically.}
}
\value{
A list with \code{matrices}, \code{responses}, \code{cohort_names}, and
\code{feature_names}. Under \code{align = "union"} the matrices are \emph{not} subset to
\code{feature_names}.
}
\description{
Shared input preparation for the feature-selection entry points. Coerces
single objects to cohort lists, extracts numeric feature matrices from
matrices / data frames / \code{SummarizedExperiment}s, standardises responses,
checks sample-count agreement, and aligns features across cohorts.
}
\details{
This is deliberately separate from \code{\link[=.prepare_cohort_inputs]{.prepare_cohort_inputs()}}, which serves
the optimisation path and additionally handles feature transforms,
\code{feature_subset}, and the richer \code{\link[=.align_features]{.align_features()}} strategies.
}
\keyword{internal}
