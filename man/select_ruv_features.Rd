% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruv_features.R
\name{select_ruv_features}
\alias{select_ruv_features}
\title{Select Features via RUV-4 Dual-Objective Scoring}
\usage{
select_ruv_features(
  x_list,
  y_list,
  n_features = 50L,
  k = NULL,
  neg_control_method = c("empirical", "list"),
  neg_control_genes = NULL,
  neg_control_quantile = 0.75,
  iterate_controls = FALSE,
  max_iterations = 3L,
  scoring = c("ratio", "composite_z"),
  epsilon = 0.01,
  ebayes = TRUE,
  return_corrected = FALSE,
  assay = NULL
)
}
\arguments{
\item{x_list}{A matrix-like object, \code{SummarizedExperiment}, or list of such
objects. Rows represent samples and columns represent features.}

\item{y_list}{A binary response aligned with \code{x_list}. Must be a factor with
levels \code{c("No", "Yes")}. Provide a list when \code{x_list} is a list.}

\item{n_features}{Maximum number of features to return (default \code{50}).}

\item{k}{Number of unwanted variation factors to estimate. When \code{NULL}
(default), auto-selected via PCA on the negative control submatrix.}

\item{neg_control_method}{Method for identifying negative control genes.
\code{"empirical"} (default) selects non-significant genes from a pooled limma
fit; \code{"list"} uses genes supplied via \code{neg_control_genes}.}

\item{neg_control_genes}{Character vector of negative control gene names.
Required when \code{neg_control_method = "list"}.}

\item{neg_control_quantile}{Quantile threshold for empirical control
selection (default \code{0.75}). Features with p-value above this quantile are
selected as controls.}

\item{iterate_controls}{If \code{TRUE}, iteratively refine the negative control
set by re-running limma on RUV-corrected expression. Default \code{FALSE}.}

\item{max_iterations}{Maximum number of control refinement iterations
(default \code{3}).}

\item{scoring}{Scoring method. \code{"ratio"} (default) uses
\code{discrimination / (alpha_norm + epsilon)}; \code{"composite_z"} uses
\code{z(discrimination) + z(-alpha_norm)}.}

\item{epsilon}{Small constant added to alpha_norm denominator in ratio
scoring (default \code{0.01}).}

\item{ebayes}{If \code{TRUE} (default), apply empirical Bayes variance shrinkage
via \code{\link[ruv:variance_adjust]{ruv::variance_adjust()}} for better-calibrated t-statistics.}

\item{return_corrected}{If \code{TRUE}, include the RUV-4-corrected expression
matrix in the output. Default \code{FALSE}.}

\item{assay}{For \code{SummarizedExperiment} inputs, the assay name or index to
extract prior to analysis.}
}
\value{
A list with components:
\describe{
\item{features}{Character vector of selected feature names.}
\item{scores}{Data frame with per-feature scoring metadata: \code{feature},
\code{discrimination}, \code{alpha_norm}, \code{score}, \code{rank}.}
\item{ruv_summary}{List of RUV-4 diagnostics: \code{k}, \code{n_controls},
\code{control_genes}, \code{W_outcome_cor}, \code{W_cohort_cor}.}
\item{corrected}{RUV-4-corrected expression matrix (only when
\code{return_corrected = TRUE}).}
\item{settings}{List of parameter values used.}
}
}
\description{
Fits RUV-4 to jointly estimate outcome-associated signal and unwanted
variation per feature, then ranks features by the ratio (or composite
z-score) of discrimination strength to unwanted-variation sensitivity.
Unlike cohort-label-based methods, RUV-4 estimates latent batch factors
from negative control genes, making it suitable when cohort structure is
ambiguous or confounders are continuous.
}
\details{
This method complements the existing cohort-label-based selectors
(\code{\link[=select_transferable_features]{select_transferable_features()}}, \code{\link[=select_discriminative_features]{select_discriminative_features()}}) by
estimating latent unwanted factors from negative control genes rather than
requiring explicit cohort labels. It is particularly useful when batch
structure is ambiguous, confounders are continuous, or multiple sources of
unwanted variation overlap.

The discrimination axis measures how strongly each feature associates with
the outcome after accounting for unwanted variation (via the RUV-4
t-statistic). The alpha_norm axis measures each feature's total loading on
the estimated unwanted factors (L2 norm across k factors). Features with
high discrimination and low alpha_norm are prioritised.
}
\seealso{
\code{\link[=select_transferable_features]{select_transferable_features()}}, \code{\link[=select_discriminative_features]{select_discriminative_features()}},
\code{\link[=select_de_features]{select_de_features()}}
}
