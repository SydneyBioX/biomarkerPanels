% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nsga_results.R
\name{.build_pareto_solutions_df}
\alias{.build_pareto_solutions_df}
\title{Build the Pareto Solutions Data Frame from an NSGA Result}
\usage{
.build_pareto_solutions_df(
  nsga_result,
  evaluate,
  objectives,
  objective_directions,
  infeasible_msg = "No solutions satisfied the supplied constraints."
)
}
\arguments{
\item{nsga_result}{The object returned by \code{rmoo::nsga2()} / \code{rmoo::nsga3()}.}

\item{evaluate}{Function mapping a decision vector to a solution list with
\code{feasible}, \code{metrics}, \code{base_features}, and \code{features} elements.}

\item{objectives}{Named list of objective specs (its names order the metric
columns).}

\item{objective_directions}{Named character vector of objective directions,
passed to \code{\link[=.filter_dominated]{.filter_dominated()}}.}

\item{infeasible_msg}{Error message used when no solution is feasible.}
}
\value{
A \code{data.frame} with \code{solution_id}, \code{base_features}/\code{features} list
columns, and one column per objective.
}
\description{
Shared post-processing for \code{\link[=optimize_panel]{optimize_panel()}} and
\code{\link[=optimize_panel_transferable]{optimize_panel_transferable()}}. Extracts the rank-1 (Pareto-optimal)
population from an \code{rmoo} result, re-evaluates each candidate to recover its
features and objective metrics, drops constraint-infeasible solutions, filters
any solutions that became dominated on re-evaluation, and assembles the
wide-format solutions data frame (one row per solution, list columns for
\code{base_features}/\code{features}, one numeric column per objective).
}
\details{
Re-evaluation can shift metrics relative to the NSGA run (e.g. non-deterministic
inner CV in glmnet), which can turn a rank-1 solution into a dominated one; the
post-filter removes those via \code{\link[=.filter_dominated]{.filter_dominated()}}.
}
\keyword{internal}
