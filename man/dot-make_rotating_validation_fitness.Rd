% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness_rotating_validation.R
\name{.make_rotating_validation_fitness}
\alias{.make_rotating_validation_fitness}
\title{Create Rotating-Validation Fitness Function}
\usage{
.make_rotating_validation_fitness(
  pool_x,
  pool_y,
  pool_cohort,
  splits,
  feature_pool,
  max_features,
  objectives,
  constraints,
  regularized,
  alpha,
  feature_transform = "none",
  min_features_required = NULL,
  selection_threshold = "adaptive",
  cache_fitness = TRUE,
  cache_max_entries = Inf
)
}
\arguments{
\item{pool_x}{Pooled base-feature matrix (train + val combined).}

\item{pool_y}{Pooled response factor.}

\item{pool_cohort}{Pooled cohort factor.}

\item{splits}{List of \verb{(train, val)} index pairs from
\code{\link[=.generate_stratified_splits]{.generate_stratified_splits()}}.}

\item{feature_pool}{Character vector of base feature names.}

\item{max_features}{Maximum base features per candidate.}

\item{objectives}{Objective list from \code{\link[=define_objectives]{define_objectives()}}.}

\item{constraints}{Constraint list.}

\item{regularized}{Whether the model is elastic-net.}

\item{alpha}{Elastic net mixing parameter.}

\item{feature_transform}{Name of the feature transform to apply.}

\item{min_features_required}{Minimum base features per candidate.}

\item{selection_threshold}{\code{"adaptive"} or a numeric gate in (0, 1).}

\item{cache_fitness}{Logical; cache duplicate selected panels during fitness.}

\item{cache_max_entries}{Maximum entries retained in the fitness cache.}
}
\value{
List with \code{wrapper} (vectorised NSGA fitness) and \code{evaluate}
(single-candidate evaluator averaged over all splits).
}
\description{
Factory returning a fitness function that rotates through precomputed
stratified train/val splits. Each NSGA batch advances the split counter;
every candidate in the batch is evaluated against the same split to
preserve Pareto dominance. The returned \code{evaluate} closure — used for
final Pareto-solution metrics — averages metrics across all K splits,
which is the honest out-of-rotation number.
}
\keyword{internal}
