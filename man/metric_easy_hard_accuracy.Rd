% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_difficulty.R
\name{metric_easy_hard_accuracy}
\alias{metric_easy_hard_accuracy}
\title{Easy-Hard Balanced Accuracy}
\usage{
metric_easy_hard_accuracy(
  truth,
  scores = NULL,
  selected = NULL,
  difficulty = NULL,
  cutoff_prob = 0.5,
  cutoff_strategy = c("fixed", "prevalence", "youden"),
  positive = "Yes"
)
}
\arguments{
\item{truth}{Binary outcome; coerced with \code{\link[=ensure_binary_response]{ensure_binary_response()}}.}

\item{scores}{Numeric scores or probabilities.}

\item{selected}{Ignored; kept for signature compatibility.}

\item{difficulty}{Factor or character vector of \code{"easy"} / \code{"hard"} labels,
same length as \code{truth}. Must be supplied.}

\item{cutoff_prob}{Classification probability cutoff applied to \code{scores}.
Ignored when \code{cutoff_strategy} is not \code{"fixed"}.}

\item{cutoff_strategy}{Strategy for computing cutoff. One of \code{"fixed"}
(use \code{cutoff_prob}), \code{"prevalence"} (cutoff = class prevalence), or
\code{"youden"} (optimal Youden's J). Default is \code{"fixed"}.}

\item{positive}{Label treated as the positive ("event") class.}
}
\value{
Balanced accuracy across easy/hard strata, between 0 and 1.
Returns \code{NA_real_} if classification is undefined in both strata.
}
\description{
Computes accuracy within each difficulty stratum (easy/hard) and returns
their mean. This rewards panels that perform well on hard-to-classify
samples rather than relying on easy cases to inflate overall accuracy.
}
\details{
The \code{difficulty} vector must be supplied via the \code{params} argument of
\code{\link[=define_objectives]{define_objectives()}} or \code{\link[=build_objectives]{build_objectives()}}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{define_objectives(
  metrics = c("easy_hard_accuracy", "num_features"),
  params = list(easy_hard_accuracy = list(difficulty = sample_labels))
)
}\if{html}{\out{</div>}}
}
\note{
The \code{difficulty} vector is captured at objective-definition time.
This works with \code{\link[=optimize_panel]{optimize_panel()}} which evaluates on the full pooled
training data. For \code{\link[=optimize_panel_transferable]{optimize_panel_transferable()}}, which subsets data
into train/validation partitions, the \code{difficulty} vector would be
misaligned with the subsetted \code{truth}---use only with the standard
pipeline.
}
