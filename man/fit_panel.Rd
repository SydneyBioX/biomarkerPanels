% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fitting.R
\name{fit_panel}
\alias{fit_panel}
\title{Fit a Panel Model from Optimization Results}
\usage{
fit_panel(
  optimization_result,
  solution_id = NULL,
  features = NULL,
  x = NULL,
  y = NULL,
  cohort = NULL,
  regularized = TRUE,
  regularized_alpha = 0.5,
  cv = FALSE,
  cv_folds = 5L,
  validate_fitness = TRUE
)
}
\arguments{
\item{optimization_result}{An \code{OptimizationResult} from \code{\link[=optimize_panel]{optimize_panel()}}.}

\item{solution_id}{Integer specifying which Pareto solution to use. If
\code{NULL} (default), automatically selects the solution with the best
value on the first objective.}

\item{features}{Character vector of features to use. If provided, overrides
the features from \code{solution_id}. Must be a subset of the feature pool.}

\item{x}{Optional training feature matrix. If \code{NULL}, uses the stored
aggregated training data from the optimization result.}

\item{y}{Optional training response vector. If \code{NULL}, uses the stored
response from the optimization result.}

\item{cohort}{Optional cohort factor. If \code{NULL}, uses the stored cohort
from the optimization result.}

\item{regularized}{Logical; if \code{TRUE} (default), use regularized regression
(elastic net via glmnet). If \code{FALSE}, use standard logistic regression.}

\item{regularized_alpha}{Elastic net mixing parameter when \code{regularized = TRUE}.
\code{alpha = 1} is lasso, \code{alpha = 0} is ridge. Default is 0.5 (elastic net).}

\item{cv}{Logical; if \code{TRUE}, use cross-validation when fitting the final
model (only applies when \code{regularized = FALSE}). Default is \code{FALSE}.}

\item{cv_folds}{Number of cross-validation folds when \code{cv = TRUE}.
Default is 5.}

\item{validate_fitness}{Logical; if \code{TRUE} (default), recomputes metrics on
the training data after model fitting to verify consistency with the
optimization results. Issues a warning if metrics differ substantially.}
}
\value{
A \code{BiomarkerPanelResult} with the fitted model.
}
\description{
Takes an \code{OptimizationResult} from \code{\link[=optimize_panel]{optimize_panel()}} and fits a model on
a selected Pareto solution. Returns a \code{BiomarkerPanelResult} with the
fitted model ready for evaluation.
}
\examples{
\dontrun{
# Run optimization
opt <- optimize_panel(x, y, objectives = define_objectives())

# Inspect solutions
summarize_solutions(opt)

# Fit model on best solution (auto-selected)
panel <- fit_panel(opt)

# Or specify a particular solution
panel <- fit_panel(opt, solution_id = 3)

# Evaluate on held-out data
evaluate_panel(panel, x_test, y_test)
}
}
\seealso{
\code{\link[=optimize_panel]{optimize_panel()}}, \code{\link[=evaluate_panel]{evaluate_panel()}}
}
