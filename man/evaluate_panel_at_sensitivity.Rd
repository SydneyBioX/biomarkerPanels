% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{evaluate_panel_at_sensitivity}
\alias{evaluate_panel_at_sensitivity}
\title{Evaluate Panel at Target Sensitivity}
\usage{
evaluate_panel_at_sensitivity(
  panel,
  x,
  y,
  target_sensitivity = 0.9,
  objectives = define_objectives(losses = c("sensitivity", "specificity", "auc")),
  positive = NULL,
  cohort_aggregator = NULL,
  assay = NULL
)
}
\arguments{
\item{panel}{A \code{BiomarkerPanelResult}.}

\item{x}{Validation feature matrix, or a list of matrices for multi-cohort
evaluation.}

\item{y}{Validation binary response factor, or a list of factors when \code{x} is
a list.}

\item{target_sensitivity}{The desired sensitivity level (default 0.90).
Must be between 0 and 1.}

\item{objectives}{Optional override for objectives. Defaults to sensitivity,
specificity, and auc.}

\item{positive}{Label treated as the positive class. Defaults to the value
stored in the panel, or \code{"Yes"}.}

\item{cohort_aggregator}{Optional aggregator name. Defaults to the aggregator
stored in the fitted panel.}

\item{assay}{For \code{SummarizedExperiment} inputs, assay name or index.}
}
\value{
Same as \code{\link[=evaluate_panel]{evaluate_panel()}}, but with \code{cutoff_prob} set to the
threshold that achieves the target sensitivity. The result also includes
a \code{target_sensitivity_info} element with details about the threshold
selection.
}
\description{
Convenience wrapper that combines \code{\link[=find_threshold_for_sensitivity]{find_threshold_for_sensitivity()}} and
\code{\link[=evaluate_panel]{evaluate_panel()}} to evaluate a panel at the operating point corresponding
to a target sensitivity level. This is the recommended way to evaluate
panels optimized with \code{\link[=loss_specificity_at_sensitivity]{loss_specificity_at_sensitivity()}}.
}
\examples{
# After optimizing with specificity_at_sensitivity:
# result <- optimize_panel(x, y, objectives = build_objectives(
#   losses = c("specificity_at_sensitivity", "num_features")
# ))
#
# Evaluate at 90\% sensitivity operating point:
# eval <- evaluate_panel_at_sensitivity(result, x_val, y_val)
}
\seealso{
\code{\link[=find_threshold_for_sensitivity]{find_threshold_for_sensitivity()}}, \code{\link[=evaluate_panel]{evaluate_panel()}}.
}
