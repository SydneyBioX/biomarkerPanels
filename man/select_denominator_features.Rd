% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/denominator_features.R
\name{select_denominator_features}
\alias{select_denominator_features}
\title{Select Denominator Features for Batch-Aware Ratio Normalization}
\usage{
select_denominator_features(
  x,
  cohort,
  y = NULL,
  n_denominators = 20L,
  n_pcs = 50L,
  batch_pvalue_threshold = 0.1,
  biology_pvalue_threshold = 0.2
)
}
\arguments{
\item{x}{Expression matrix (samples x genes). Should contain log-transformed
expression values.}

\item{cohort}{Factor of cohort/batch labels.}

\item{y}{Binary outcome labels for biology filtering. Set to \code{NULL} to skip
biology filtering (denominators selected purely on batch association).}

\item{n_denominators}{Number of denominator genes to select (default 20).}

\item{n_pcs}{Number of principal components to analyze (default 50).}

\item{batch_pvalue_threshold}{P-value threshold for batch association. PCs
with ANOVA p < this threshold are considered batch-associated (default
0.10).}

\item{biology_pvalue_threshold}{P-value threshold for biology non-association.
PCs with biology p > this threshold are considered biology-free (default
0.20).}
}
\value{
A list with components:
\describe{
\item{denominators}{Character vector of selected denominator gene names.}
\item{diagnostics}{Data frame with per-PC statistics including variance
explained, batch p-value/R^2, and biology p-value/R^2.
}
\item{problematic_cohort}{The cohort most associated with batch effects.}
\item{one_vs_rest}{Data frame with per-cohort batch separation metrics.}
\item{pure_batch_pcs}{Integer vector of PC indices identified as pure batch.}
\item{gene_scores}{Data frame with per-gene batch and biology scores.}
\item{pca}{The \code{prcomp} object from PCA on input data.}
}
}
\description{
Identifies genes that are associated with batch/cohort effects but NOT with
the biological outcome. These serve as "noise proxy" genes that can normalize
signal genes through log-ratios: \code{log(Signal) - log(Denominator)} cancels
batch-specific scaling factors.
}
\details{
The algorithm proceeds as follows:
\enumerate{
\item PCA on combined expression matrix
\item Test each PC for batch association (ANOVA) and biology association (if
\code{y} provided)
\item Identify "pure batch" PCs: batch-significant AND biology-nonsignificant
\item If no pure batch PCs found, fall back to LDA-based cohort discrimination
\item Extract genes with highest loadings on pure batch PCs
\item Score genes by ratio of batch to biology contribution
\item Return top n_denominators genes
}
}
\examples{
\dontrun{
# Assume x is samples x genes, cohort and y are factors
result <- select_denominator_features(
  x = expression_matrix,
  cohort = cohort_labels,
  y = disease_labels,
  n_denominators = 20
)

# Use denominators with construct_dual_stream_ratios()
ratios <- construct_dual_stream_ratios(
  x = expression_matrix,
  numerators = signal_genes,
  denominators = result$denominators
)
}

}
