% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_feature_helpers.R
\name{.prepare_scoring_matrix}
\alias{.prepare_scoring_matrix}
\title{Validate and Transform Selected Base Features}
\usage{
.prepare_scoring_matrix(
  x_raw,
  base_features,
  feature_transform,
  context = "data"
)
}
\arguments{
\item{x_raw}{Raw feature matrix (samples x base features) with column names.}

\item{base_features}{Character vector of base feature names to select.}

\item{feature_transform}{Registered transform name, or \code{"none"}.}

\item{context}{Human-readable location for the not-found error message
(e.g. \code{"validation data"}, \code{"held-out data"}).}
}
\value{
List with \code{x_selected} (transformed matrix) and \code{selected}
(transformed feature names).
}
\description{
Checks that a panel's base features are present in \code{x_raw}, subsets to them,
and applies the panel's feature transform on the fly. Callers are responsible
for resolving \code{base_features} / \code{feature_transform} from the panel (some
allow an explicit transform override) and for any downstream subsetting to a
model's expected column subset (e.g. CPOP panels).
}
\keyword{internal}
