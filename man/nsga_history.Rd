% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_class.R
\name{nsga_history}
\alias{nsga_history}
\alias{nsga_history,OptimizationResult-method}
\title{NSGA Generation History}
\usage{
nsga_history(object)

\S4method{nsga_history}{OptimizationResult}(object)
}
\arguments{
\item{object}{An \code{OptimizationResult}.}
}
\value{
A data frame with one row per individual per generation. Columns:
\code{generation} (integer), \code{individual} (row in the population),
\code{rank} (integer NSGA rank, 1 = Pareto-optimal), \code{is_pareto} (logical),
one column per objective with values in the user-facing direction (i.e.
maximize objectives are reported on their original scale, not the
negated minimization form rmoo uses internally), \code{n_features} (panel
size), and \code{base_features} (a list-column of character vectors giving
the base feature names selected for each individual). Returns an empty
data frame if no history was recorded.
}
\description{
Retrieve the per-generation NSGA population recorded during optimization.
Only populated when \code{\link[=optimize_panel]{optimize_panel()}} was called with
\code{record_history = TRUE}.
}
\section{Methods (by class)}{
\itemize{
\item \code{nsga_history(OptimizationResult)}: Return the per-generation NSGA history data frame.

}}
