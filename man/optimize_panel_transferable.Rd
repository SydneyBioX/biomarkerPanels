% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_panel_transferable.R
\name{optimize_panel_transferable}
\alias{optimize_panel_transferable}
\title{Optimize Biomarker Panels with Transferability Focus}
\usage{
optimize_panel_transferable(
  x,
  y,
  objectives = define_objectives(losses = c("sensitivity", "specificity",
    "num_features")),
  max_features = 5L,
  feature_pool = NULL,
  train_ratio = 0.7,
  val_ratio = 0.2,
  np_alpha = 0.15,
  np_delta = 0.05,
  cohort_aggregator = "pairwise_ratios",
  feature_alignment = "intersection",
  constraints = list(),
  algorithm = c("NSGA-III", "NSGA-II"),
  nsga_control = list(),
  assay = NULL,
  seed = NULL,
  regularized = TRUE,
  regularized_alpha = 0.5,
  n_top_features = 50L
)
}
\arguments{
\item{x}{List of matrix-like objects representing cohorts.}

\item{y}{List of binary response vectors aligned with \code{x}.}

\item{objectives}{Named list of objective descriptors as returned by
\code{\link[=define_objectives]{define_objectives()}}.}

\item{max_features}{Maximum number of biomarkers permitted in a panel.}

\item{feature_pool}{Optional subset of feature identifiers. If \code{NULL},
\code{select_transferable_features()} is run on training data only.}

\item{train_ratio}{Proportion of each cohort for training (default 0.7).}

\item{val_ratio}{Proportion of each cohort for validation (default 0.2).}

\item{np_alpha}{Type I error rate for Neyman-Pearson threshold (default 0.15).}

\item{np_delta}{Tolerance for NP threshold selection (default 0.05).}

\item{cohort_aggregator}{Transformation applied to cohort matrices (default
\code{"pairwise_ratios"}).}

\item{feature_alignment}{Strategy for aligning features across cohorts
(default \code{"intersection"}).}

\item{constraints}{Optional list of constraint descriptors.}

\item{algorithm}{Multi-objective algorithm: \code{"NSGA-III"} (default) or
\code{"NSGA-II"}.}

\item{nsga_control}{Named list of arguments passed to rmoo.}

\item{assay}{For \code{SummarizedExperiment} inputs, assay name or index.}

\item{seed}{Optional integer seed for reproducibility.}

\item{regularized}{Logical; if \code{TRUE} (default), use regularized regression.}

\item{regularized_alpha}{Elastic net mixing parameter (default 0.5).}

\item{n_top_features}{Number of top features to select via
\code{select_transferable_features()} when \code{feature_pool} is NULL (default 50).}
}
\value{
A \code{TransferablePanelResult} with Pareto-optimal solutions, NP

threshold, per-cohort metrics, and weighted variance.
}
\description{
Wrapper implementing a train/validation/held-out split strategy with
Neyman-Pearson threshold selection for cross-cohort generalizability.
Unlike \code{\link[=optimize_panel]{optimize_panel()}}, this function partitions each cohort into
three sets: training (for NSGA optimization), validation (for solution
selection), and held-out (for NP threshold calibration and final metrics).
}
