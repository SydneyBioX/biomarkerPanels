% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregators.R
\name{aggregator_reference_norm}
\alias{aggregator_reference_norm}
\title{Reference normalization aggregator}
\usage{
aggregator_reference_norm(x)
}
\arguments{
\item{x}{A numeric matrix with column names. Must have a \code{"reference_feature"}
attribute specifying which column to use as the reference.}
}
\value{
Matrix with (p - 1) columns, one for each non-reference feature.
Column names follow the pattern "Feature--Reference".
}
\description{
Normalizes all features relative to a specified reference feature (e.g., a
housekeeping gene). The reference feature must be specified via the
\code{"reference_feature"} attribute on the input matrix.
}
\examples{
\dontrun{
x <- matrix(rnorm(30), nrow = 10, ncol = 3)
colnames(x) <- c("GeneA", "GeneB", "Housekeeping")
attr(x, "reference_feature") <- "Housekeeping"
result <- aggregator_reference_norm(x)
}

}
\keyword{internal}
