% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypervolume.R
\name{hypervolume}
\alias{hypervolume}
\alias{hypervolume,OptimizationResult-method}
\title{Hypervolume of an Optimization Result}
\usage{
hypervolume(object, ref = NULL, ref_buffer = 0.05)

\S4method{hypervolume}{OptimizationResult}(object, ref = NULL, ref_buffer = 0.05)
}
\arguments{
\item{object}{An \code{OptimizationResult}.}

\item{ref}{Optional numeric reference point in the \strong{user-facing} direction
(same scale as the columns in \code{solutions(object)}). If \code{NULL}, defaults to
the front's nadir shifted by \code{ref_buffer}.}

\item{ref_buffer}{Fractional outward shift applied per objective when \code{ref}
is auto-computed. Default \code{0.05}.}
}
\value{
A single numeric value: the dominated hypervolume in minimization
form. The reference point used is attached as \code{attr(., "ref")} (still in
user-facing direction).
}
\description{
Compute the dominated hypervolume of the Pareto front contained in an
\code{OptimizationResult}. Hypervolume is a standard quality indicator for
multi-objective optimization: larger values indicate a front that
simultaneously improves on more objectives.
}
\details{
Objective values are converted to a common minimization form using the
directions stored in \code{control$objective_directions} (i.e. \code{"maximize"}
objectives are negated). The reference point must therefore strictly dominate
every point in minimization form. By default the nadir of the observed front
is used and shifted outward by \code{ref_buffer * range} per objective so all
points contribute non-zero volume.

Requires the suggested \code{emoa} package.
}
\section{Methods (by class)}{
\itemize{
\item \code{hypervolume(OptimizationResult)}: Hypervolume of the final Pareto front.

}}
\seealso{
\code{\link[=hypervolume_history]{hypervolume_history()}} for HV trajectory across NSGA generations.
}
