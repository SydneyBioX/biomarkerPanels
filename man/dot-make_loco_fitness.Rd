% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness_loco.R
\name{.make_loco_fitness}
\alias{.make_loco_fitness}
\title{Create Leave-One-Cohort-Out Fitness Function}
\usage{
.make_loco_fitness(
  x,
  y,
  cohort,
  feature_pool,
  max_features,
  objectives,
  constraints,
  regularized,
  alpha,
  feature_transform = "none",
  min_features_required = NULL,
  selection_threshold = "adaptive",
  cache_fitness = TRUE,
  cache_max_entries = Inf
)
}
\arguments{
\item{x}{Pooled base-feature matrix (samples x features).}

\item{y}{Pooled response factor.}

\item{cohort}{Factor aligned with rows of \code{x}; must have >=2 levels.}

\item{feature_pool}{Character vector of base feature names (cols of \code{x}).}

\item{max_features}{Maximum base features per candidate.}

\item{objectives}{Objective list from \code{\link[=define_objectives]{define_objectives()}}.}

\item{constraints}{Constraint list.}

\item{regularized}{Whether the per-fold model is elastic-net.}

\item{alpha}{Elastic net mixing parameter.}

\item{feature_transform}{Name of the feature transform to apply on-the-fly.}

\item{min_features_required}{Minimum base features per candidate.}

\item{selection_threshold}{\code{"adaptive"} or a numeric gate in (0, 1).}

\item{cache_fitness}{Logical; cache duplicate selected panels during fitness.}

\item{cache_max_entries}{Maximum entries retained in the fitness cache.}
}
\value{
List with \code{wrapper} (vectorized NSGA fitness) and \code{evaluate}
(single-candidate evaluator returning full diagnostics).
}
\description{
Factory that scores each candidate by iterating over cohorts: train on all
cohorts except one, predict on the held-out cohort, concatenate
out-of-cohort predictions, and compute objective metrics on the pooled
OOF scores. This forces the optimizer to select features that transfer
across cohort boundaries rather than features that simply fit within-cohort
validation samples.
}
\keyword{internal}
